# Build artifacts
**/bin/
**/obj/
**/node_modules/
**/dist/
**/.angular/

# IDE / editor
**/.vs/
**/.vscode/
*.user

# Secrets — never commit real values, only .env.example
.env
.env.edge

# Offline-first / cloud-sync: per-node identity + key material (mounted volume in prod)
Backend/keys/
**/keys/node-id.txt

# Unrelated project bundled in this folder
dfc_app/
dfc_app.zip

# Local tool state
.claude/

# Runtime-uploaded content (persisted via Docker volume instead)
Backend/wwwroot/uploads/
